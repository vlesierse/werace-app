steps:
  - uses: actions/setup-dotnet@v4
    with:
      dotnet-version: "10.0.x"
  - uses: actions/setup-node@v4
    with:
      node-version: "22"
  - run: dotnet restore src/api/WeRace.slnx
  - run: cd src/app && npm install
